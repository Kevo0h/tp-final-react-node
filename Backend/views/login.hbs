<style>
    .bg-transparente { background-color: rgba(255, 255, 255, 0.8); } /* Ajusta la opacidad para que sea un poco más visible */
  </style>
</head>
<body class="bg-info d-flex align-items-center justify-content-center" style="height: 100vh;">

  <div class="container  d-flex align-items-center justify-content-center">
    <div class="bg-transparente p-5 rounded shadow" style="max-width: 500px;">
      <h1 class="text-center text-dark">Gestión de Reclamos</h1>
      <form action="/login" method="post" class="d-flex flex-column">
        <div class="form-group">
          <label for="usuario">Usuario:</label>
          <input
            type="text"
            class="form-control"
            id="usuario"
            name="usuario"
            required
          />
        </div>
        <div class="form-group">
          <label for="password">Contraseña:</label>
          <input
            type="password"
            class="form-control"
            id="password"
            name="password"
            required
          />
        </div>
        <button
          type="submit"
          class="btn btn-success"
        >Ingresar</button>
      </form>
    </div>
  </div>

  {{#if error}}
    <p> usuario y/o contraseña Incorrecto</p>

  {{/if}}

  {{message}}

</body>